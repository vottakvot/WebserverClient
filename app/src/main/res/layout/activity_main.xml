<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/common_white_background"
    tools:context="com.example.user.webserverclient.MainActivity">

    <Button
        style="@style/ButtonCommon"
        android:id="@+id/refresh_ip_list"
        android:layout_alignParentTop="true"
        android:background="@drawable/drawable_borders"
        android:layout_marginBottom="20dp"
        android:text="@string/refresh_ip"/>

    <TextView
        style="@style/TextCommon"
        android:id="@+id/count_name"
        android:layout_below="@id/refresh_ip_list"
        android:text="@string/count_name"/>

    <TextView
        style="@style/TextCommon"
        android:id="@+id/count_count"
        android:layout_toRightOf="@id/count_name"
        android:layout_below="@id/refresh_ip_list"
        android:text="@string/count_count"/>

    <ListView
        style="@style/ListCommon"
        android:id="@+id/ip_list"
        android:layout_below="@id/count_name"
        android:background="@drawable/drawable_borders"/>

</RelativeLayout>
